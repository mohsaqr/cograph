% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-nestimate.R
\name{plot_netobject_ml}
\alias{plot_netobject_ml}
\alias{plot.netobject_ml}
\title{Plot a Multilevel Nestimate netobject}
\usage{
plot_netobject_ml(
  x,
  layout = NULL,
  common_scale = TRUE,
  titles = c("Between-person", "Within-person"),
  ...
)

\method{plot}{netobject_ml}(x, ...)
}
\arguments{
\item{x}{A \code{netobject_ml} object with \code{$between} and \code{$within} networks.}

\item{layout}{Character: layout algorithm. Default \code{"oval"} (deterministic).}

\item{common_scale}{Logical: use the same maximum weight for both panels? Default TRUE.}

\item{titles}{Character vector of length 2: panel titles. Default
\code{c("Between-person", "Within-person")}.}

\item{...}{Additional arguments passed to \code{splot()}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Creates a side-by-side plot for a \code{netobject_ml} object, showing the
between-person and within-person networks.
}
\examples{
mat <- matrix(c(0, .5, .3, .5, 0, .4, .3, .4, 0), 3, 3)
colnames(mat) <- rownames(mat) <- c("A", "B", "C")
btw <- as_cograph(mat)
wth <- as_cograph(mat * 0.6)
ml <- structure(list(between = btw, within = wth), class = c("netobject_ml", "list"))
plot_netobject_ml(ml)
}
