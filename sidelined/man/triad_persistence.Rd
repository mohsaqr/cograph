% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifs-temporal.R
\name{triad_persistence}
\alias{triad_persistence}
\title{Analyze Triad Persistence Across Time Windows}
\usage{
triad_persistence(
  x,
  by = c("triad", "type"),
  edge_weight = FALSE,
  min_windows = 1,
  min_persistence = 0.5
)
}
\arguments{
\item{x}{A \code{cograph_temporal_motifs} object from \code{\link[=extract_motifs_temporal]{extract_motifs_temporal()}}}

\item{by}{Aggregation level: \code{"triad"} (default) tracks specific labeled triads
(e.g., "Plan - Execute - Monitor"), \code{"type"} aggregates by MAN type (e.g., "021C").
Use \code{"type"} to see how many triads of each structural pattern exist per window.}

\item{edge_weight}{Logical. If \code{TRUE}, weight counts by the number of edges in the triad.
A full bidirectional clique (6 edges) weights 2x, while a minimal triad (3 edges) weights 1x.
Formula: \code{count * (n_edges / 3)}. Default \code{FALSE} for raw counts.}

\item{min_windows}{Minimum number of windows a triad/type must appear in to be
included in the results. Default 1 (include all).}

\item{min_persistence}{Minimum persistence score (0-1) to be classified as
"persistent". Default 0.5 (appears in at least half the windows).}
}
\value{
A \code{cograph_triad_persistence} object containing:
\describe{
\item{triads}{Data frame with columns: triad/type label, type (MAN type),
n_windows (appearances), first_window, last_window, windows (comma-separated),
persistence (proportion), total_count, status}
\item{counts_matrix}{Numeric matrix (rows x windows) with observed counts per cell.
When \code{by = "type"}, counts show how many triads of that type exist per window.}
\item{presence_matrix}{Binary matrix showing presence/absence}
\item{window_totals}{Total number of triads observed per window (for normalization)}
\item{summary}{List with counts by status}
\item{params}{List of parameters used, including \code{by}}
}
}
\description{
Identify which specific labeled triads or MAN types persist, emerge, or fade across
temporal windows in a motif analysis.
}
\section{Status Classification}{

\describe{
\item{persistent}{Appears in at least \code{min_persistence} proportion of windows}
\item{transient}{Appears in only 1 window}
\item{emerging}{First appears after window 1 AND last appears in final window}
\item{fading}{First appears in window 1 AND disappears before final window}
\item{sporadic}{Other patterns (gaps in appearance)}
}
}

\examples{
\dontrun{
library(tna)
data <- group_regulation

# Extract temporal motifs
m <- extract_motifs_temporal(data, window_size = 5, step = 2, pattern = "all")

# By specific triads (default)
pers <- triad_persistence(m)
print(pers)
plot(pers, type = "heatmap", top_n = 20)

# By MAN type
pers_type <- triad_persistence(m, by = "type")
plot(pers_type, type = "heatmap")
}

}
\seealso{
\code{\link[=extract_motifs_temporal]{extract_motifs_temporal()}}, \code{\link[=plot.cograph_triad_persistence]{plot.cograph_triad_persistence()}},
\code{\link[=extract_motifs]{extract_motifs()}} for single-network motif analysis

Other motifs: 
\code{\link{extract_motifs}()},
\code{\link{extract_motifs_temporal}()},
\code{\link{extract_triads}()},
\code{\link{get_edge_list}()},
\code{\link{motif_census}()},
\code{\link{plot.cograph_motif_analysis}()},
\code{\link{plot.cograph_motifs}()},
\code{\link{plot.cograph_temporal_motifs}()},
\code{\link{plot.cograph_triad_persistence}()},
\code{\link{triad_census}()}
}
\concept{motifs}
