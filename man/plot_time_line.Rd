% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-timeline.R
\name{plot_time_line}
\alias{plot_time_line}
\title{Plot Cluster Timeline Network}
\usage{
plot_time_line(
  x = NULL,
  cluster_list = NULL,
  layers = NULL,
  orientation = c("horizontal", "vertical"),
  mode = c("weights", "tna"),
  aggregation = c("sum", "mean", "max"),
  minimum = 0.02,
  between_minimum = 0.1,
  spacing = 4,
  shape_size = 1.8,
  node_radius_scale = 0.85,
  node_size = 1.8,
  colors = NULL,
  shell_alpha = 0.15,
  shell_border_width = 2,
  node_border_color = "gray30",
  edge_width_range = c(0.3, 1.3),
  edge_alpha = 0.35,
  arrows = TRUE,
  arrow_size = 0.06,
  between_curve_strength = 0.8,
  between_alpha = 0.12,
  between_lwd_range = c(0.3, 1.5),
  show_labels = TRUE,
  label_size = 0.5,
  label_color = "gray20",
  label_abbrev = NULL,
  cluster_labels = TRUE,
  cluster_label_size = 1.1,
  edge_labels = FALSE,
  edge_label_size = 0.5,
  edge_label_color = "gray40",
  edge_label_digits = 2,
  layer_gap = NULL,
  layer_label_size = 1.2,
  layer_label_color = "gray30",
  cross_layer_alpha = 0.4,
  cross_layer_lwd = 0.6,
  global_scale = TRUE,
  legend = TRUE,
  legend_position = "bottom",
  legend_size = 0.9,
  title = NULL,
  subtitle = NULL,
  title_size = 1.4,
  nodes = NULL,
  ...
)
}
\arguments{
\item{x}{Weight matrix, tna object, cograph_network, or cluster_summary
object. Used for single-layer mode. Ignored when \code{layers} is provided.}

\item{cluster_list}{Named list of node vectors per cluster. Required for
matrix input. Ignored if x is cluster_summary.}

\item{layers}{Named list of weight matrices (or tna/cograph_network objects)
for multi-layer mode. Each element is one layer (e.g., time point).
When provided, \code{x} is ignored. Default NULL (single-layer mode).}

\item{orientation}{Layout direction: "horizontal" (clusters left-to-right)
or "vertical" (clusters top-to-bottom). Default "horizontal".}

\item{mode}{What values to use: "weights" (raw aggregated) or "tna"
(row-normalized). Default "weights".}

\item{aggregation}{How to aggregate: "sum", "mean", "max". Default "sum".
Ignored if x is cluster_summary.}

\item{minimum}{Edge weight threshold for within-cluster edges. Default 0.02.}

\item{between_minimum}{Edge weight threshold for between-cluster edges.
Default 0.10. Increase to show fewer between-cluster connections.}

\item{spacing}{Distance between cluster centers. Default 4.}

\item{shape_size}{Cluster shell size. Default 1.8.}

\item{node_radius_scale}{Node ring radius within cluster as proportion of
shape_size. Default 0.85.}

\item{node_size}{Node circle size. Default 1.8.}

\item{colors}{Cluster colors. Default NULL (auto palette).}

\item{shell_alpha}{Shell fill transparency. Default 0.15.}

\item{shell_border_width}{Shell border line width. Default 2.}

\item{node_border_color}{Node border color. Default "gray30".}

\item{edge_width_range}{Min/max within-cluster edge width. Default c(0.3, 1.3).}

\item{edge_alpha}{Within-cluster edge transparency. Default 0.35.}

\item{arrows}{Show arrows on within-cluster edges. Default TRUE.}

\item{arrow_size}{Arrow head size. Default 0.06.}

\item{between_curve_strength}{How much between-cluster curves bow outward.
Default 0.8.}

\item{between_alpha}{Between-cluster edge transparency. Default 0.12.}

\item{between_lwd_range}{Min/max between-cluster edge width. Default
c(0.3, 1.5).}

\item{show_labels}{Show node labels. Default TRUE.}

\item{label_size}{Label text size. Default 0.5.}

\item{label_color}{Label text color. Default "gray20".}

\item{label_abbrev}{Label abbreviation: NULL (none), integer (max chars),
or "auto". Default NULL.}

\item{cluster_labels}{Show cluster names. Default TRUE.}

\item{cluster_label_size}{Cluster label text size. Default 1.1.}

\item{edge_labels}{Show weight labels on within-cluster edges. Default FALSE.}

\item{edge_label_size}{Edge label text size. Default 0.5.}

\item{edge_label_color}{Edge label text color. Default "gray40".}

\item{edge_label_digits}{Decimal places for edge labels. Default 2.}

\item{layer_gap}{Vertical gap between layers in multi-layer mode. Default
NULL (auto-calculated).}

\item{layer_label_size}{Layer name text size (multi-layer). Default 1.2.}

\item{layer_label_color}{Layer name text color. Default "gray30".}

\item{cross_layer_alpha}{Cross-layer dashed connection transparency.
Default 0.4.}

\item{cross_layer_lwd}{Cross-layer dashed connection line width.
Default 0.6.}

\item{global_scale}{Use global weight scaling across all layers. Default TRUE.}

\item{legend}{Show legend. Default TRUE.}

\item{legend_position}{Legend position: "bottom", "top", "right", "left",
"none". Default "bottom".}

\item{legend_size}{Legend text size. Default 0.9.}

\item{title}{Main title. Default NULL.}

\item{subtitle}{Subtitle. Default NULL.}

\item{title_size}{Title text size. Default 1.4.}

\item{nodes}{Node metadata data frame. Default NULL.}

\item{...}{Unused.}
}
\value{
Invisibly returns a cluster_summary (single-layer) or list of
cluster_summary objects (multi-layer).
}
\description{
Horizontal or vertical layout of clusters with within-cluster detail
networks and curved between-cluster edges connecting adjacent clusters.
Supports multi-layer mode for visualizing the same clustered network
across multiple time points or conditions.
}
\examples{
# Create test matrix
mat <- matrix(runif(36), 6, 6)
diag(mat) <- 0
colnames(mat) <- rownames(mat) <- LETTERS[1:6]
clusters <- list(Group1 = c("A", "B", "C"), Group2 = c("D", "E", "F"))

# Horizontal (default)
plot_time_line(mat, clusters)

# Vertical
plot_time_line(mat, clusters, orientation = "vertical")

# Multi-layer
mat2 <- matrix(runif(36), 6, 6)
diag(mat2) <- 0
colnames(mat2) <- rownames(mat2) <- LETTERS[1:6]
plot_time_line(layers = list("Time 1" = mat, "Time 2" = mat2),
               cluster_list = clusters)
}
\seealso{
\code{\link{cluster_summary}}, \code{\link{plot_mcml}}
}
