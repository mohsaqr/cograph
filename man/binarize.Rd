% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle-weights.R
\name{binarize}
\alias{binarize}
\title{Binarize Edge Weights}
\usage{
binarize(
  x,
  threshold = 0,
  absolute = TRUE,
  signed = FALSE,
  keep_isolates = TRUE,
  keep_format = FALSE,
  directed = NULL
)
}
\arguments{
\item{x}{Network input.}

\item{threshold}{Numeric. Edges whose weight exceeds this value are kept and
set to 1. Default 0, which keeps every existing edge.}

\item{absolute}{Logical. Compare \code{abs(weight)}. Default TRUE, so a
correlation network keeps its strong negative edges.}

\item{signed}{Logical. If TRUE, negative edges become \code{-1} rather than
\code{1}, preserving the sign of the association. Default FALSE.}

\item{keep_isolates}{Logical. Keep nodes that end up with no edges? Default TRUE.}

\item{keep_format}{Logical. Return the input format when TRUE.}

\item{directed}{Logical or NULL. If NULL (default), auto-detect.}
}
\value{
A \code{cograph_network} whose weights are all 1 (or \code{-1} when
\code{signed = TRUE}), or the input format when \code{keep_format = TRUE}.
}
\description{
Replaces every surviving weight with 1, dropping edges at or below the
threshold. The network equivalent of \code{sna::event2dichot()}.
}
\examples{
adj <- matrix(c(0, .5, .8, 0,
                .5, 0, .3, .6,
                .8, .3, 0, .4,
                 0, .6, .4, 0), 4, 4, byrow = TRUE)
rownames(adj) <- colnames(adj) <- c("A", "B", "C", "D")

binarize(adj, threshold = 0.45)
}
\references{
Butts, C. T. (2008). Social network analysis with sna.
\emph{Journal of Statistical Software}, 24(6), 1--51.
}
\seealso{
\code{\link{threshold_edges}}, \code{\link{normalize_weights}}
}
