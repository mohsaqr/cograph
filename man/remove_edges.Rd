% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle-edit.R
\name{remove_edges}
\alias{remove_edges}
\title{Remove Edges from a Network}
\usage{
remove_edges(
  x,
  from,
  to,
  keep_isolates = TRUE,
  keep_format = FALSE,
  directed = NULL
)
}
\arguments{
\item{x}{Network input.}

\item{from}{Source nodes, by label or index.}

\item{to}{Target nodes, by label or index. The same length as \code{from}.}

\item{keep_isolates}{Logical. Keep nodes that end up with no edges? Default
TRUE, matching \code{\link{filter_edges}}.}

\item{keep_format}{Logical. Return the input format when TRUE.}

\item{directed}{Logical or NULL. If NULL (default), auto-detect.}
}
\value{
A \code{cograph_network} without those edges, or the input format
when \code{keep_format = TRUE}. Named pairs that carry no edge are
reported in a \code{cograph_no_such_edge} warning.
}
\description{
Remove Edges from a Network
}
\examples{
adj <- matrix(c(0, 1, 1, 1, 0, 1, 1, 1, 0), 3, 3)
rownames(adj) <- colnames(adj) <- c("A", "B", "C")

remove_edges(adj, from = "A", to = "B")
}
\seealso{
\code{\link{add_edges}}, \code{\link{filter_edges}},
\code{\link{remove_isolates}}
}
