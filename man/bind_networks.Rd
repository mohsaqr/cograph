% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle-edit.R
\name{bind_networks}
\alias{bind_networks}
\title{Combine Two Networks}
\usage{
bind_networks(
  x,
  y,
  method = c("union", "intersection", "difference"),
  weight = c("sum", "mean", "max", "min", "first"),
  keep_format = FALSE,
  directed = NULL
)
}
\arguments{
\item{x, y}{Network inputs.}

\item{method}{How to combine the edge sets:
\describe{
\item{\code{"union"}}{(default) every edge of either network, over the
union of the node sets}
\item{\code{"intersection"}}{only edges present in both, over the nodes
common to both}
\item{\code{"difference"}}{edges of \code{x} that are not in \code{y},
over the nodes of \code{x}}
}}

\item{weight}{How to combine the weights of an edge present in both:
\code{"sum"} (default), \code{"mean"}, \code{"max"}, \code{"min"}, or
\code{"first"} (keep \code{x}'s weight).}

\item{keep_format}{Logical. Return \code{x}'s format when TRUE.}

\item{directed}{Logical or NULL. If NULL (default), the result is directed
when either input is.}
}
\value{
A \code{cograph_network} over the combined node set, or \code{x}'s
format when \code{keep_format = TRUE}. Nodes are ordered with \code{x}'s
first, then any node only \code{y} has.
}
\description{
Aligns two networks on node labels and combines their edges.
}
\examples{
a <- matrix(0, 3, 3, dimnames = list(c("A", "B", "C"), c("A", "B", "C")))
a["A", "B"] <- a["B", "A"] <- 1
b <- matrix(0, 3, 3, dimnames = list(c("B", "C", "D"), c("B", "C", "D")))
b["B", "C"] <- b["C", "B"] <- 2

bind_networks(a, b)
bind_networks(a, b, method = "difference")
}
\seealso{
\code{\link{add_edges}}, \code{\link{plot_difference}}
}
