% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-utils.R
\name{to_matrix}
\alias{to_matrix}
\title{Convert Network to Adjacency Matrix}
\usage{
to_matrix(x, directed = NULL)
}
\arguments{
\item{x}{Network input: matrix, cograph_network, igraph, network, tna, etc.}

\item{directed}{Logical or NULL. If NULL (default), auto-detect from input.}
}
\value{
A square numeric adjacency matrix with row/column names.
}
\description{
Converts any supported network format to an adjacency matrix.
}
\examples{
# From matrix
adj <- matrix(c(0, .5, .8, 0,
                .5, 0, .3, .6,
                .8, .3, 0, .4,
                 0, .6, .4, 0), 4, 4, byrow = TRUE)
rownames(adj) <- colnames(adj) <- c("A", "B", "C", "D")
to_matrix(adj)

# From cograph_network
net <- as_cograph(adj)
to_matrix(net)

# From igraph
\dontrun{
g <- igraph::make_ring(5)
to_matrix(g)
}
}
\seealso{
\code{\link{to_igraph}}, \code{\link{to_df}}, \code{\link{as_cograph}},
\code{\link{to_network}}
}
