% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle-structure.R
\name{spanning_tree}
\alias{spanning_tree}
\title{Minimum or Maximum Spanning Tree}
\usage{
spanning_tree(
  x,
  weights = c("weight", "none"),
  maximum = FALSE,
  keep_format = FALSE,
  directed = NULL
)
}
\arguments{
\item{x}{Network input.}

\item{weights}{\code{"weight"} (default) uses the edge weights as costs;
\code{"none"} treats every edge as cost 1.}

\item{maximum}{Logical. Find the maximum spanning tree instead of the
minimum. Default FALSE. Set TRUE when the weights are similarities.}

\item{keep_format}{Logical. Return the input format when TRUE.}

\item{directed}{Logical or NULL. Directedness to read the input with; the
tree itself is undirected.}
}
\value{
An undirected \code{cograph_network} holding the spanning tree (or
forest), or the input format when \code{keep_format = TRUE}. Every node is
kept.
}
\description{
Prim's algorithm on each connected component, so a disconnected network
yields a spanning forest.
}
\examples{
adj <- matrix(c(0, .5, .8, 0,
                .5, 0, .3, .6,
                .8, .3, 0, .4,
                 0, .6, .4, 0), 4, 4, byrow = TRUE)
rownames(adj) <- colnames(adj) <- c("A", "B", "C", "D")

spanning_tree(adj)
spanning_tree(adj, maximum = TRUE)
}
\references{
Prim, R. C. (1957). Shortest connection networks and some generalizations.
\emph{Bell System Technical Journal}, 36(6), 1389--1401.
}
\seealso{
\code{\link{disparity_filter}}, \code{\link{threshold_edges}}
}
