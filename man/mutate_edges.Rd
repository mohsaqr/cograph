% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle-edit.R
\name{mutate_edges}
\alias{mutate_edges}
\title{Add or Change Edge Attributes}
\usage{
mutate_edges(
  x,
  ...,
  community = "louvain",
  keep_format = FALSE,
  directed = NULL
)
}
\arguments{
\item{x}{Network input.}

\item{...}{Named expressions evaluated against the edge table, with the same
metrics and predicates \code{\link{select_edges}()} offers, for example
\code{strong = abs_weight > 0.5} or \code{scaled = weight / max(weight)}.}

\item{community}{Community detection method used when an expression refers
to \code{same_community}, \code{from_community} or \code{to_community}.
Default \code{"louvain"}.}

\item{keep_format}{Logical. Return the input format when TRUE.}

\item{directed}{Logical or NULL. If NULL (default), auto-detect.}
}
\value{
A \code{cograph_network} whose edge table has the new columns, or
the input format when \code{keep_format = TRUE}.
}
\description{
Add or Change Edge Attributes
}
\examples{
adj <- matrix(c(0, .5, .8, 0,
                .5, 0, .3, .6,
                .8, .3, 0, .4,
                 0, .6, .4, 0), 4, 4, byrow = TRUE)
rownames(adj) <- colnames(adj) <- c("A", "B", "C", "D")

as.data.frame(mutate_edges(adj, strong = weight > 0.5))
}
\seealso{
\code{\link{mutate_nodes}}, \code{\link{select_edges}}
}
