% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle-edit.R
\name{add_nodes}
\alias{add_nodes}
\title{Add Nodes to a Network}
\usage{
add_nodes(x, labels, ..., keep_format = FALSE, directed = NULL)
}
\arguments{
\item{x}{Network input.}

\item{labels}{Character vector of labels for the new nodes.}

\item{...}{Named vectors of node attributes for the new nodes, each of
length 1 (recycled) or \code{length(labels)}. Columns the network does not
already have are created and filled with \code{NA} for the existing nodes.}

\item{keep_format}{Logical. Return the input format when TRUE.}

\item{directed}{Logical or NULL. If NULL (default), auto-detect.}
}
\value{
A \code{cograph_network} with the new nodes appended (isolated until
edges are added), or the input format when \code{keep_format = TRUE}.
}
\description{
Add Nodes to a Network
}
\examples{
adj <- matrix(c(0, 1, 1, 0), 2, 2)
rownames(adj) <- colnames(adj) <- c("A", "B")

add_nodes(adj, labels = c("C", "D"))
add_nodes(adj, labels = "C", group = "new")
}
\seealso{
\code{\link{remove_nodes}}, \code{\link{add_edges}},
\code{\link{mutate_nodes}}
}
