% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-network.R
\name{as.data.frame.cograph_network}
\alias{as.data.frame.cograph_network}
\title{Cograph Network as a Data Frame}
\usage{
\method{as.data.frame}{cograph_network}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("edges", "nodes")
)
}
\arguments{
\item{x}{A \code{cograph_network} object.}

\item{row.names}{\code{NULL} or a character vector of row names, as for
\code{\link[base]{as.data.frame}}.}

\item{optional}{Logical, as for \code{\link[base]{as.data.frame}}. Ignored;
the column names of the returned table are always the documented ones.}

\item{...}{Unused, for compatibility with the generic.}

\item{what}{Which table to return. \code{"edges"} (default) or
\code{"nodes"}.}
}
\value{
A base data frame. For \code{what = "edges"}, one row per edge with
columns \code{from} and \code{to} (node labels), \code{weight}, and any
extra edge columns the network carries (for example \code{session}). For
\code{what = "nodes"}, one row per node with the node metadata columns
(\code{id}, \code{label}, layout coordinates, and any custom columns).
}
\description{
The tidy accessor for a \code{cograph_network}: one row per edge (or per
node), with endpoints given as labels rather than internal indices, so no
caller has to reach into the object with \code{$} or translate integer ids
by hand.
}
\examples{
adj <- matrix(c(0, .5, .8, 0,
                .5, 0, .3, .6,
                .8, .3, 0, .4,
                 0, .6, .4, 0), 4, 4, byrow = TRUE)
rownames(adj) <- colnames(adj) <- c("A", "B", "C", "D")
net <- as_cograph(adj)

as.data.frame(net)
as.data.frame(net, what = "nodes")
}
\seealso{
\code{\link{to_df}}, \code{\link{get_edges}}, \code{\link{get_nodes}}
}
