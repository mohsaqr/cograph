% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle-edit.R
\name{add_edges}
\alias{add_edges}
\title{Add Edges to a Network}
\usage{
add_edges(x, from, to, weight = 1, ..., keep_format = FALSE, directed = NULL)
}
\arguments{
\item{x}{Network input.}

\item{from}{Source nodes, by label or index.}

\item{to}{Target nodes, by label or index. The same length as \code{from}.}

\item{weight}{Numeric weight for the new edges, length 1 or
\code{length(from)}. Default 1.}

\item{...}{Named vectors of extra edge attributes, length 1 or
\code{length(from)}.}

\item{keep_format}{Logical. Return the input format when TRUE.}

\item{directed}{Logical or NULL. If NULL (default), auto-detect.}
}
\value{
A \code{cograph_network} with the new edges, or the input format
when \code{keep_format = TRUE}. An edge that already exists has its weight
replaced, and a \code{cograph_edges_replaced} warning says how many.
}
\description{
Add Edges to a Network
}
\note{
When the igraph package is attached it masks this function with
\code{igraph::add_edges()}, which takes an igraph object. Use
\code{cograph::add_edges()} to be explicit.
}
\examples{
adj <- matrix(0, 3, 3, dimnames = list(LETTERS[1:3], LETTERS[1:3]))
adj["A", "B"] <- adj["B", "A"] <- 1

add_edges(adj, from = "B", to = "C", weight = 0.5)
}
\seealso{
\code{\link{remove_edges}}, \code{\link{add_nodes}},
\code{\link{bind_networks}}
}
