% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-temporal.R
\name{plot_temporal}
\alias{plot_temporal}
\title{Temporal Network Prism (3D Glass Box)}
\usage{
plot_temporal(
  x,
  time = NULL,
  slices = NULL,
  cumulative = FALSE,
  labels = NULL,
  layout = "spring",
  node_size = 2.5,
  node_color = "steelblue",
  node_shape = 21,
  node_border = "gray30",
  edge_color = "#E41A1C",
  edge_width = 1.5,
  edge_alpha = 0.35,
  plane_color = "gray92",
  plane_alpha = 0.2,
  plane_border = "gray60",
  plane_lty = 2,
  box = TRUE,
  box_color = "gray40",
  connections = FALSE,
  connection_color = "gray50",
  connection_alpha = 0.15,
  minimum = 0,
  show_labels = FALSE,
  label_size = 0.4,
  title = NULL,
  angle = c(1, 0.7),
  seed = 42,
  ...
)
}
\arguments{
\item{x}{An edge list data frame with columns \code{from}, \code{to}, and
a time column, OR a \code{cograph_network} (reads time from stored data),
OR a named list of network objects.}

\item{time}{Character. Name of the time column.}

\item{slices}{Integer or NULL. Number of equal-width time bins. Default
NULL uses unique time values.}

\item{cumulative}{Logical. If TRUE, edges accumulate. Default FALSE.}

\item{labels}{Character vector of layer labels. Default auto.}

\item{layout}{Character or matrix. Shared node layout. Default
\code{"spring"}.}

\item{node_size}{Numeric. Node size. Default 2.5.}

\item{node_color}{Character or vector. Node fill color. A single color
applies to all layers, or a vector of length \code{n_layers} for
per-layer colors. Default \code{"steelblue"}.}

\item{node_shape}{Integer. Point shape (\code{pch}). Default 21 (filled
circle).}

\item{node_border}{Character. Node border color. Default \code{"gray30"}.}

\item{edge_color}{Character or vector. Edge color (single or per-layer).
Default \code{"#E41A1C"}.}

\item{edge_width}{Numeric. Base edge width. Actual width scales by
weight. Default 1.5.}

\item{edge_alpha}{Numeric. Edge transparency (0-1). Default 0.35.}

\item{plane_color}{Character or vector. Plane fill color (single or
per-layer). Default \code{"gray92"}.}

\item{plane_alpha}{Numeric. Plane fill transparency (0-1). Default 0.2.}

\item{plane_border}{Character. Plane border color. Default
\code{"gray60"}.}

\item{plane_lty}{Integer. Plane border line type. Default 2 (dashed).}

\item{box}{Logical. Draw 3D bounding box. Default TRUE.}

\item{box_color}{Character. Box edge color. Default \code{"gray40"}.}

\item{connections}{Logical. Draw lines connecting same nodes across
planes. Default FALSE.}

\item{connection_color}{Character. Default \code{"gray50"}.}

\item{connection_alpha}{Numeric. Default 0.15.}

\item{minimum}{Numeric. Minimum edge weight to display. Default 0.}

\item{show_labels}{Logical. Default FALSE.}

\item{label_size}{Numeric. Label text size. Default 0.4.}

\item{title}{Character or NULL. Plot title. Default NULL.}

\item{angle}{Numeric vector of length 2: \code{c(dz_x, dz_y)} controlling
the oblique projection shear. Default \code{c(1.0, 0.7)}.}

\item{seed}{Integer or NULL. Default 42.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisible list of adjacency matrices per layer.
}
\description{
Displays a network at different time points as vertical planes inside a
3D oblique-projection box, with time flowing left to right. Each network
plane extends into the depth of the box.
}
\examples{
\dontrun{
edges$week <- sample(1:3, nrow(edges), replace = TRUE)
cograph::plot_temporal(edges, time = "week")

# Customized
cograph::plot_temporal(edges, time = "week",
  node_color = "coral", edge_color = "gray30",
  node_size = 3, edge_width = 2, plane_lty = 3,
  angle = c(1.2, 0.8), title = "Classroom Evolution")
}
}
\seealso{
\code{\link{plot_network_evolution}}, \code{\link{plot_mlna}}
}
