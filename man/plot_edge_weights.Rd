% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-distributions.R
\name{plot_edge_weights}
\alias{plot_edge_weights}
\title{Plot Edge Weight Distribution}
\usage{
plot_edge_weights(
  x,
  normalize = FALSE,
  bins = NULL,
  log = "",
  directed = NULL,
  col = "steelblue",
  border = "white",
  main = "Edge Weight Distribution",
  xlab = "Weight",
  ...
)
}
\arguments{
\item{x}{Network input: matrix, igraph, network, cograph_network, or tna.}

\item{normalize}{Logical. Show proportions. Default FALSE.}

\item{bins}{Integer or NULL. Number of bins. Default NULL (auto).}

\item{log}{Character. Log scaling. Default \code{""}.}

\item{directed}{Logical or NULL. Default NULL (auto-detect).}

\item{col}{Fill color. Default \code{"steelblue"}.}

\item{border}{Border color. Default \code{"white"}.}

\item{main}{Title. Default \code{"Edge Weight Distribution"}.}

\item{xlab}{X-axis label. Default \code{"Weight"}.}

\item{...}{Additional arguments passed to \code{\link[graphics]{barplot}}.}
}
\value{
Invisibly returns the weight vector.
}
\description{
Histogram of edge weights in a network.
}
\examples{
adj <- matrix(c(0, 2, 3, 2, 0, 1, 3, 1, 0), 3, 3)
rownames(adj) <- colnames(adj) <- c("A", "B", "C")
cograph::plot_edge_weights(adj)
}
