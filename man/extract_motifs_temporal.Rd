% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifs-temporal.R
\name{extract_motifs_temporal}
\alias{extract_motifs_temporal}
\title{Extract Motifs Across Time Windows}
\usage{
extract_motifs_temporal(
  x,
  id = NULL,
  time = NULL,
  from = NULL,
  to = NULL,
  state = NULL,
  format = c("auto", "wide", "edge_list", "long"),
  window_size = NULL,
  step = 1,
  pattern = c("triangle", "network", "closed", "all"),
  edge_method = c("any", "expected", "percent"),
  edge_threshold = 1.5,
  min_transitions = 5,
  exclude_types = NULL,
  include_types = NULL,
  na_threshold = 0.5,
  seed = NULL
)
}
\arguments{
\item{x}{Input data. Can be:
\itemize{
\item A wide data.frame (rows=individuals, columns=time points)
\item An edge list data.frame (with from, to, time columns)
\item A long format data.frame (with id, time, state columns)
\item A \code{tna_windows} result from \code{\link[=tna_windows]{tna_windows()}}
}}

\item{id}{Column name(s) for grouping (character). For wide format, separates
ID columns from time columns. For edge_list/long format, identifies individuals.}

\item{time}{Column name for time (edge_list/long format). Default "time".}

\item{from}{Column name for source node (edge_list format). Default "from".}

\item{to}{Column name for target node (edge_list format). Default "to".}

\item{state}{Column name for state (long format). Default "state".}

\item{format}{Input format: "auto" (detect), "wide", "edge_list", or "long".}

\item{window_size}{Number of time points per window. Default depends on format:
1 for edge_list, 2 for wide/long.}

\item{step}{Window step size. 1 = sliding (default), window_size = tumbling.}

\item{pattern}{Pattern filter: "triangle", "network", "closed", or "all".
See \code{\link[=extract_motifs]{extract_motifs()}} for details.}

\item{edge_method}{Method for edge presence: "any", "expected", or "percent".}

\item{edge_threshold}{Threshold for expected/percent methods. Default 1.5.}

\item{min_transitions}{Minimum transitions per individual/window. Default 5.}

\item{exclude_types}{Character vector of MAN types to exclude.}

\item{include_types}{Character vector of MAN types to exclusively include.}

\item{na_threshold}{Maximum NA proportion before stopping. Default 0.5.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A \code{cograph_temporal_motifs} object containing:
\describe{
\item{windows}{List of per-window motif results}
\item{summary}{Data frame with window, start, end, type, count}
\item{type_trends}{Data frame of type counts over time (wide format)}
\item{params}{Parameters used}
}
}
\description{
Analyze how network motifs evolve over temporal sequences by extracting
and counting triad patterns within rolling time windows.
}
\examples{
\dontrun{
library(tna)

# Wide format (default)
data <- group_regulation
m <- extract_motifs_temporal(data, window_size = 5, step = 1)
print(m)
plot(m, type = "trends")
plot(m, type = "heatmap")

# Edge list format
edges <- data.frame(
  from = c("A", "B", "A", "C", "B", "A"),
  to = c("B", "C", "C", "A", "A", "B"),
  time = c(1, 1, 2, 2, 3, 3)
)
m <- extract_motifs_temporal(edges, from = "from", to = "to", time = "time",
                              window_size = 2)
}

}
\seealso{
\code{\link[=extract_motifs]{extract_motifs()}}, \code{\link[=tna_windows]{tna_windows()}}, \code{\link[=plot.cograph_temporal_motifs]{plot.cograph_temporal_motifs()}},
\code{\link[=triad_persistence]{triad_persistence()}} for persistence analysis

Other motifs: 
\code{\link{extract_motifs}()},
\code{\link{extract_triads}()},
\code{\link{get_edge_list}()},
\code{\link{motif_census}()},
\code{\link{plot.cograph_motif_analysis}()},
\code{\link{plot.cograph_motifs}()},
\code{\link{plot.cograph_temporal_motifs}()},
\code{\link{plot.cograph_triad_persistence}()},
\code{\link{triad_census}()},
\code{\link{triad_persistence}()}
}
\concept{motifs}
