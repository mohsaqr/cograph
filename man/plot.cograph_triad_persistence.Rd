% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifs-temporal.R
\name{plot.cograph_triad_persistence}
\alias{plot.cograph_triad_persistence}
\title{Plot Triad Persistence Analysis}
\usage{
\method{plot}{cograph_triad_persistence}(
  x,
  type = c("heatmap", "timeline", "status"),
  top_n = NULL,
  fill = c("density", "binary"),
  normalize = FALSE,
  show_counts = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{cograph_triad_persistence} object from \code{\link[=triad_persistence]{triad_persistence()}}}

\item{type}{Plot type: "heatmap" (presence across windows, default),
"timeline" (first/last appearance), or "status" (status distribution bar chart)}

\item{top_n}{Maximum number of triads to display. Default \code{NULL} shows all triads.
Set to an integer to limit (e.g., \code{top_n = 20} for top 20).}

\item{fill}{For heatmap type only: "density" (default) colors cells by actual count
normalized globally across all cells, "binary" shows simple present/absent coloring.}

\item{normalize}{Logical. If \code{TRUE}, normalize counts by the total number of triads
in each window. Default is \code{FALSE} for raw counts.}

\item{show_counts}{Logical. If \code{TRUE}, display count values inside heatmap cells.
Default is \code{TRUE} when matrix is small enough (< 200 cells), \code{FALSE} otherwise.}

\item{...}{Additional arguments (unused)}
}
\value{
A ggplot2 object (invisibly)
}
\description{
Create visualizations for triad persistence analysis including heatmaps,
timelines, and status distribution charts.
}
\examples{
\dontrun{
library(tna)
m <- extract_motifs_temporal(group_regulation, window_size = 5)

pers <- triad_persistence(m)
plot(pers, type = "heatmap", top_n = 20)

pers_type <- triad_persistence(m, by = "type")
plot(pers_type, type = "heatmap")
plot(pers_type, type = "heatmap", normalize = TRUE)

plot(pers, type = "timeline")
plot(pers, type = "status")
}

}
\seealso{
\code{\link[=triad_persistence]{triad_persistence()}} for the analysis that produces this object,
\code{\link[=extract_motifs_temporal]{extract_motifs_temporal()}} for temporal motif extraction

Other motifs: 
\code{\link{extract_motifs}()},
\code{\link{extract_motifs_temporal}()},
\code{\link{extract_triads}()},
\code{\link{get_edge_list}()},
\code{\link{motif_census}()},
\code{\link{plot.cograph_motif_analysis}()},
\code{\link{plot.cograph_motifs}()},
\code{\link{plot.cograph_temporal_motifs}()},
\code{\link{triad_census}()},
\code{\link{triad_persistence}()}
}
\concept{motifs}
